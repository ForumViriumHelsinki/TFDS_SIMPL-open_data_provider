apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.argocd.appname }}
  namespace: {{ .Values.argocd.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  syncPolicy:
    automated: {}
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
      - ServerSideApply=true
  project: {{ .Values.project }}
  destination:
    server: {{ .Values.cluster.address }}
    namespace: {{ .Values.cluster.namespace }}
  sources:

  - repoURL: {{ .Values.values.repo_URL }}
    targetRevision: {{ .Values.values.branch }}
    ref: values

  - repoURL: {{ .Values.keycloak.repo_URL }}
    targetRevision: {{ .Values.keycloak.targetRevision }}
    chart: {{ .Values.keycloak.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.keycloak.valueFiles }}
        - $values/app-values/Keycloak/{{ . }}
        {{- end }}
      values: |
        global:
          security:
            allowInsecureImages: true
        image:
          repository: keycloak/keycloak
          tag: 26.4
        defaultInitContainers:
          prepareWriteDirs:
            enabled: false
        args:
          - start
          - --import-realm
          - --verbose
          - --proxy-headers
          - xforwarded
        containerSecurityContext:
          runAsUser: 1000
          runAsNonRoot: true
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
        fullnameOverride: {{ .Values.keycloak.nameOverride }}
        apiUrl: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
        podAnnotations:
          vault.security.banzaicloud.io/vault-addr: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
          vault.security.banzaicloud.io/vault-role: {{ .Values.secrets.role }}
        auth:
          adminPassword: vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-keycloak#admin
        externalDatabase:
          host: "pg-cluster.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local"
          database: "{{ .Values.namespaceTag.dataprovider }}_keycloak"
          user: "{{ .Values.namespaceTag.dataprovider }}_keycloak"
          password: vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-postgres-passwords#{{ .Values.cluster.namespace }}-keycloak
        extraEnvVars:
        - name: KC_HOSTNAME_ADMIN
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/auth"
        - name: KC_HOSTNAME
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/auth"
        - name: USERS_ROLES_BASE_URL
          value: "http://users-roles.{{ .Values.cluster.namespace }}.svc.cluster.local:8080/tier1/v2"
        - name: AUTHENTICATION_PROVIDER_BASE_URL
          value: "http://authentication-provider.{{ .Values.cluster.namespace }}.svc.cluster.local:8080/tier1/v2"
        - name: KEYCLOAK_BASE_URL
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/auth"
        - name: REALM
          value: "participant"
        - name: KEYCLOAK_ADMIN
          value: "admin"
        - name: KEYCLOAK_ADMIN_PASSWORD
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-keycloak#admin"
        - name: KC_DB
          value: "postgres"
        - name: KC_DB_URL_HOST
          value: "pg-cluster.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local"
        - name: KC_DB_URL_PORT
          value: "5432"
        - name: KC_DB_URL_DATABASE
          value: "{{ .Values.namespaceTag.dataprovider }}_keycloak"
        - name: KC_DB_USERNAME
          value: "{{ .Values.namespaceTag.dataprovider }}_keycloak"
        - name: KC_DB_PASSWORD
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-postgres-passwords#{{ .Values.cluster.namespace }}-keycloak"
        - name: KC_BOOTSTRAP_ADMIN_USERNAME
          value: "admin"
        - name: KC_BOOTSTRAP_ADMIN_PASSWORD
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-keycloak#admin"
        extraVolumes:
          - name: spi-volume
            emptyDir: {}
          - name: realm-volume
            configMap:
              name: {{ .Values.keycloak.chart_name }}-realms
        extraVolumeMounts:
          - name: spi-volume
            mountPath: /opt/keycloak/providers
          - name: realm-volume
            mountPath: /opt/keycloak/data/import
        initContainers:
        - name: init-spi
          image: curlimages/curl
          command: ["/bin/sh", "-c"]
          env:
            - name: ARTIFACT
              value: keycloak-authenticator
            - name: URL
              value: https://code.europa.eu/api/v4/projects/915/packages/maven/eu/europa/ec/simpl/keycloak-authenticator/{{ .Values.keycloak.keycloak_authenticator.targetRevision }}/keycloak-authenticator-{{ .Values.keycloak.keycloak_authenticator.targetRevision }}.jar
            - name: EIDAS_EXTENSION_ARTIFACT
              value: keycloak-eidas-demo-authenticator
            - name: EIDAS_EXTENSION_URL
              value: https://code.europa.eu/api/v4/projects/1313/packages/maven/eu/europa/ec/simpl/eidas-demo-keycloak-extension/{{ .Values.keycloak.eidas_demo_keycloak_extension.targetRevision }}/eidas-demo-keycloak-extension-{{ .Values.keycloak.eidas_demo_keycloak_extension.targetRevision }}.jar
          args:
            - |
              curl -o /custom-spi/${ARTIFACT}.jar ${URL};
              curl -o /custom-spi/${EIDAS_EXTENSION_ARTIFACT}.jar ${EIDAS_EXTENSION_URL};
          volumeMounts:
            - name: spi-volume
              mountPath: /custom-spi
        {{- if .Values.keycloak.resourcesPreset }}
        resourcesPreset: {{ .Values.keycloak.resourcesPreset }}
        {{- end }}
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.keycloak.resources }}
        resources: {{- toYaml .Values.keycloak.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.auth_provider.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.auth_provider.targetRevision }}
    chart: {{ .Values.auth_provider.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.auth_provider.valueFiles }}
        - $values/app-values/auth-provider/{{ . }}
        {{- end }}
      values: |
        podAnnotations:
          vault.security.banzaicloud.io/vault-addr: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
          vault.security.banzaicloud.io/vault-role: {{ .Values.secrets.role }}
        appConfig:
          management:
            otlp:
              tracing:
                endpoint: http://collector.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}
          spring:
            datasource:
              url: "jdbc:postgresql://pg-cluster.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:5432/{{ .Values.cluster.namespace }}_authenticationprovider"
              username: "{{ .Values.namespaceTag.dataprovider }}_authenticationprovider"
              {{- with .Values.backendExtraGlobalDatasource }}
              {{ . | nindent 14 }}
              {{- end }}
            data:
              redis:
                host: "redis-master.{{ .Values.cluster.namespace }}.svc.cluster.local"
          microservice:
            identity-provider:
              url: http://identity-provider.{{ .Values.cluster.namespace }}.svc.cluster.local:8080
          client:
            authority:
              url: "https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"
          simpl:
            certificate:
              san: "tls.participant.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
          openid-connect:
            certsEndpoint: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/auth/realms/participant/protocol/openid-connect/certs"

        env:
        - name: LOGGING_LEVEL_EU_EUROPA_EC_SIMPL
          value: "DEBUG"
        - name: SPRING_JPA_SHOW_SQL
          value: "false"
        - name: KAFKA_AUTHPROVIDER
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.commonToolsNamespace }}-kafka-credentials#{{ .Values.cluster.namespace }}_authprovider"
        - name: SPRING_DATA_REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-secrets
              key: redis
        - name: SPRING_DATASOURCE_PASSWORD
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-postgres-passwords#{{ .Values.cluster.namespace }}-authenticationprovider"
        kafkaConfig:
          spring:
            kafka:
              bootstrap-servers: kafka.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:9092
              producer:
                properties:
                  security.protocol: SASL_PLAINTEXT
                  sasl.mechanism: PLAIN
                  sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="{{ .Values.cluster.namespace }}_authprovider" password="${KAFKA_AUTHPROVIDER}";
              admin:
                auto-create: true
          simpl:
            kafka:
              topic:
                prefix: iaa.{{ .Values.cluster.namespace }}.
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.auth_provider.resources }}
        resources: {{- toYaml .Values.auth_provider.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.users_roles.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.users_roles.targetRevision }}
    chart: {{ .Values.users_roles.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.users_roles.valueFiles }}
        - $values/app-values/Users-Roles/{{ . }}
        {{- end }}
      values: |
        podAnnotations:
          vault.security.banzaicloud.io/vault-addr: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
          vault.security.banzaicloud.io/vault-role: {{ .Values.secrets.role }}
        hostT1: participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        authorityHostT2: tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}
        global:
          ingress:
            issuer: {{ .Values.cluster.issuer }}
        keycloak:
          master:
            password: vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-keycloak#admin
        db:
          url: "jdbc:postgresql://pg-cluster.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:5432/{{ .Values.cluster.namespace }}_usersroles"
          username: "{{ .Values.namespaceTag.dataprovider }}_usersroles"
          password: vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-postgres-passwords#{{ .Values.cluster.namespace }}-usersroles
        appConfig:
          spring:
            datasource:
              {{- with .Values.backendExtraGlobalDatasource }}
              {{ . | nindent 14 }}
              {{- end }}
          notification:
            role-requests:
              my-profile-url: https://participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/my-profile
              role-request-review-page: https://participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/role-requests-management/review/{roleRequestId}
        env:
        - name: LOGGING_LEVEL_EU_EUROPA_EC_SIMPL
          value: "DEBUG"
        - name: SPRING_JPA_SHOW_SQL
          value: "false"
        - name: KAFKA_USERSROLES
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.commonToolsNamespace }}-kafka-credentials#{{ .Values.cluster.namespace }}_usersroles"
        microservices:
          authenticationProviderUrl: http://authentication-provider.{{ .Values.cluster.namespace }}.svc.cluster.local:8080
        kafkaConfig:
          spring:
            kafka:
              bootstrap-servers: kafka.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:9092
              producer:
                properties:
                  security.protocol: SASL_PLAINTEXT
                  sasl.mechanism: PLAIN
                  sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="{{ .Values.cluster.namespace }}_usersroles" password="${KAFKA_USERSROLES}";
              consumer:
                group-id: users-roles
                properties:
                  security.protocol: SASL_PLAINTEXT
                  sasl.mechanism: PLAIN
                  sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="{{ .Values.cluster.namespace }}_usersroles" password="${KAFKA_USERSROLES}";
              admin:
                auto-create: true
          simpl:
            kafka:
              topic:
                prefix: iaa.{{ .Values.cluster.namespace }}.
              inbox:
                topic:
                  pattern: iaa\.{{ .Values.cluster.namespace }}\..*
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.users_roles.resources }}
        resources: {{- toYaml .Values.users_roles.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.users_roles_fe.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.users_roles_fe.targetRevision }}
    chart: {{ .Values.users_roles_fe.chart_name }}
    helm:
      values: |
        hostFe: participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        ingress:
          issuer: {{ .Values.cluster.issuer }}
        cors:
          allowOrigin: https://participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},http://localhost:4202,http://localhost:4203,http://localhost:3000
        env:
        - name: API_URL
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
        - name: KEYCLOAK_URL
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/auth"
        - name: KEYCLOAK_REALM
          value: "participant"
        - name: KEYCLOAK_CLIENT_ID
          value: "frontend-cli"
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.users_roles_fe.resources }}
        resources: {{- toYaml .Values.users_roles_fe.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.authentication_provider_fe.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.authentication_provider_fe.targetRevision }}
    chart: {{ .Values.authentication_provider_fe.chart_name }}
    helm:
      values: |
        hostFe: participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        ingress:
          issuer: {{ .Values.cluster.issuer }}
        cors:
          allowOrigin: https://participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},http://localhost:4202,http://localhost:4203,http://localhost:3000
        env:
        - name: API_URL
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
        - name: KEYCLOAK_URL
          value: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/auth"
        - name: KEYCLOAK_REALM
          value: "participant"
        - name: KEYCLOAK_CLIENT_ID
          value: "frontend-cli"
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.authentication_provider_fe.resources }}
        resources: {{- toYaml .Values.authentication_provider_fe.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.tier1_gateway.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.tier1_gateway.targetRevision }}
    chart: {{ .Values.tier1_gateway.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.tier1_gateway.valueFiles }}
        - $values/app-values/tier1-gateway/{{ . }}
        {{- end }}
      values: |
        hostT1: participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        cors:
          allowOrigin: https://participant.fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},https://infrastructure-fe.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }},http://localhost:4202,http://localhost:3000
        ingress:
          issuer: {{ .Values.cluster.issuer }}
        keycloakUrl: http://keycloak.{{ .Values.cluster.namespace }}.svc.cluster.local
        usersRolesUrl: http://users-roles.{{ .Values.cluster.namespace }}.svc.cluster.local:8080
        authenticationProviderUrl: http://authentication-provider.{{ .Values.cluster.namespace }}.svc.cluster.local:8080
        env:
        - name: LOGGING_LEVEL_EU_EUROPA_EC_SIMPL
          value: "DEBUG"
        - name: SPRING_JPA_SHOW_SQL
          value: "false"
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.tier1_gateway.resources }}
        resources: {{- toYaml .Values.tier1_gateway.resources | nindent 10 }}
          {{- end }}
        {{- end }}
        appConfig:
          external-routes:
            spring-route-definitions:
              - id: sdtooling-wizard-be
                uri: http://sd-creation-wizard-api:8080
                predicates:
                  - Path=/sdtooling-api/**
                filters:
                  - StripPrefix=1
              - id: xfsc-advsearch-be
                uri: http://xfsc-advsearch-be:8080
                predicates:
                  - Path=/xfsc-advsearch-be/**
                filters:
                  - StripPrefix=1
              - id: edc-connector
                uri: http://edc-connector-adapter:8080
                predicates:
                  - Path=/edc-connector-adapter/**
                filters:
                  - StripPrefix=1
              - id: signer
                uri: http://signer:8080
                predicates:
                  - Path=/signer/**
                filters:
                  - StripPrefix=1
              - id: infra-be
                uri: http://infrastructure-be-api-svc
                predicates:
                  - Path=/infra-be/**
                filters:
                  - StripPrefix=1
              - id: edc-management
                uri: http://simpl-edc-service:29193
                predicates:
                  - Path=/edc/**
                filters:
                  - StripPrefix=1
              - id: asset-orchestrator-V1
                uri: http://{{ .Values.cluster.namespace }}-dataprovider-orchestration-platform-asset-orches.{{ .Values.cluster.namespace }}.svc.cluster.local:8080
                predicates:
                  - Path=/asset-orchestrator/**
                filters:
                  - StripPrefix=1
              - id: dagster-V1
                uri: http://{{ .Values.cluster.namespace }}-dataprovider-orchestration-platform-oauth2-proxy.{{ .Values.cluster.namespace }}.svc.cluster.local:4180
                predicates:
                  - Path=/dagster/**
            public-urls:
              - method: "GET"
                path: "/dagster/**"
              - method: "POST"
                path: "/dagster/**"
              - method: "GET"
                path: "/actuator/health/**"
              - method: GET
                path: "/sdtooling-api/swagger-ui/**"
              - method: GET
                path: "/sdtooling-api/api-docs/**"
              - method: GET
                path: "/sdtooling-api/status"
          jwt-configuration:
            primary:
              realm: participant

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.tier2_gateway.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.tier2_gateway.targetRevision }}
    chart: {{ .Values.tier2_gateway.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.tier2_gateway.valueFiles }}
        - $values/app-values/tier2-gateway/{{ . }}
        {{- end }}
      values: |
        podAnnotations:
          vault.security.banzaicloud.io/vault-addr: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
          vault.security.banzaicloud.io/vault-role: {{ .Values.secrets.role }}
        env:
        - name: LOGGING_LEVEL_EU_EUROPA_EC_SIMPL
          value: "DEBUG"
        - name: SPRING_JPA_SHOW_SQL
          value: "false"
        - name: KAFKA_TIER2GW
          value: "vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.commonToolsNamespace }}-kafka-credentials#{{ .Values.cluster.namespace }}_tier2gw"
        - name: SPRING_DATA_REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis-secrets
              key: redis
        cors:
            allowOrigin: https://authority.fe.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }},https://authority.be.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }},http://localhost:4202,http://localhost:3000
        kafka:
          bootstrap-servers: kafka.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:9092
        redis:
          host: "redis-master.{{ .Values.cluster.namespace }}.svc.cluster.local"
        hostT2: "tls.participant.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
        authorityUrlT1: "https://authority.be.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"
        kafkaConfig:
          spring:
            kafka:
              bootstrap-servers: kafka.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:9092
              consumer:
                properties:
                  security.protocol: SASL_PLAINTEXT
                  sasl.mechanism: PLAIN
                  sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="{{ .Values.cluster.namespace }}_tier2gw" password="${KAFKA_TIER2GW}";
              admin:
                auto-create: true
          simpl:
            kafka:
              topic:
                prefix: iaa.{{ .Values.cluster.namespace }}.
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.tier2_gateway.resources }}
        resources: {{- toYaml .Values.tier2_gateway.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.tier2_proxy.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.tier2_proxy.targetRevision }}
    chart: {{ .Values.tier2_proxy.chart_name }}
    helm:
      values: |
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.tier2_proxy.resources }}
        resources: {{- toYaml .Values.tier2_proxy.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: {{ .Values.redis.repo_URL }}
    targetRevision: {{ .Values.redis.targetRevision }}
    chart: {{ .Values.redis.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.redis.valueFiles }}
        - $values/app-values/Redis/{{ . }}
        {{- end }}
      values: |
        fullnameOverride: {{ .Values.redis.nameOverride }}
        master:
          podAnnotations:
            vault.security.banzaicloud.io/vault-addr: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
            vault.security.banzaicloud.io/vault-role: {{ .Values.secrets.role }}
          {{- if .Values.redis.resourcesPreset }}
          resourcesPreset: {{ .Values.redis.resourcesPreset }}
          {{- end }}
          {{- if eq .Values.resourcePreset "low" }}
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
          {{- else }}
            {{- if .Values.redis.resources }}
          resources: {{- toYaml .Values.redis.resources | nindent 10 }}
            {{- end }}
          {{- end }}

  {{- if .Values.redis_commander.enabled }}
  - repoURL: {{ .Values.redis_commander.repo_URL }}
    targetRevision: {{ .Values.redis_commander.targetRevision }}
    path: {{ .Values.redis_commander.path }}
    helm:
      valueFiles:
        {{- range .Values.redis_commander.valueFiles }}
        - $values/app-values/Redis_commander/{{ . }}
        {{- end }}
      values: |
        fullnameOverride: {{ .Values.redis_commander.nameOverride }}
        ingress:
          hosts:
          - host: redis-commander.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
            paths:
              - "/"
          tls:
          - secretName: redis-commander-tls
            hosts:
              - redis-commander.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
          annotations:
            cert-manager.io/cluster-issuer: {{ .Values.cluster.issuer }}
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.redis_commander.resources }}
        resources: {{- toYaml .Values.redis_commander.resources | nindent 10 }}
          {{- end }}
        {{- end }}
  {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.signer.projectID }}/packages/helm/stable"
      targetRevision: {{ .Values.signer.targetRevision }}
      chart: {{ .Values.signer.chart_name }}
      helm:
        valueFiles:
          {{- range .Values.signer.valueFiles }}
          - $values/app-values/Signer/{{ . }}
          {{- end }}
        values: |
          serviceAccount:
            name: "{{ .Values.signer.serviceAccountName }}"
          ingress:
            enabled: false
            frontendDomain: signer.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
            annotations:
              cert-manager.io/cluster-issuer: {{ .Values.cluster.issuer }}
          hashicorp:
            service: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
            role: {{ .Values.secrets.role }}
          secretEngine: /v1/{{ .Values.secrets.secretEngine }}/data
          secretConfigPath: "/v1/{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-infra-adapter-simpl-backend"
          {{- if eq .Values.resourcePreset "low" }}
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
          {{- else }}
            {{- if .Values.signer.resources }}
          resources: {{- toYaml .Values.signer.resources | nindent 10 }}
            {{- end }}
          {{- end }}

    - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.simpl_catalogue_client.projectID }}/packages/helm/stable"
      targetRevision: {{ .Values.simpl_catalogue_client.targetRevision }}
      chart: {{ .Values.simpl_catalogue_client.chart_name }}
      helm:
        valueFiles:
          {{- range .Values.simpl_catalogue_client.valueFiles }}
          - $values/app-values/simpl-catalogue-client/{{ . }}
          {{- end }}
        values: |
          env:
            PUBLIC_AUTH_KEYCLOAK_SERVER_URL: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
            PUBLIC_FC_DATA_URL: "https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}/fc"
            PUBLIC_SEARCH_API_URL: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/xfsc-advsearch-be"
            PUBLIC_CONTRACT_CONSUMPTION_API_URL: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/contract-consumption"
          ingress:
            hosts:
              - host: catalogue-ui.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
                paths:
                - path: /
                  pathType: ImplementationSpecific
            tls:
              - secretName: catalogue-ui-tld-secret
                hosts:
                  - catalogue-ui.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
            annotations:
              cert-manager.io/cluster-issuer: {{ .Values.cluster.issuer }}
          {{- if eq .Values.resourcePreset "low" }}
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
          {{- else }}
            {{- if .Values.simpl_catalogue_client.resources }}
          resources: {{- toYaml .Values.simpl_catalogue_client.resources | nindent 10 }}
            {{- end }}
          {{- end }}

    - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.simpl_edc.projectID }}/packages/helm/stable"
      targetRevision: {{ .Values.simpl_edc.targetRevision }}
      chart: {{ .Values.simpl_edc.chart_name }}
      helm:
        releaseName: {{ .Values.simpl_edc.nameOverride }}
        valueFiles:
          {{- range .Values.simpl_edc.valueFiles }}
          - $values/app-values/simpl-edc/{{ . }}
          {{- end }}
        values: |
          deployment:
            host: {{ .Values.simpl_edc.nameOverride }}-service
            callbackAddress: "https://tls.participant.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/edc"
          env:
            datasourceurl: "jdbc:postgresql://pg-cluster.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:5432/{{ .Values.cluster.namespace }}_edc"
            edcParticipantId: {{ .Values.cluster.namespace }}
          postgresql:
            username: {{ .Values.cluster.namespace }}_edc
          hashicorp:
            role: {{ .Values.secrets.role }}
            service: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
          secretEngine: /v1/{{ .Values.secrets.secretEngine }}/data
          secretConfigPath: "/v1/{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-simpl-edc"
          {{- if eq .Values.resourcePreset "low" }}
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
          {{- else }}
            {{- if .Values.simpl_edc.resources }}
          resources: {{- toYaml .Values.simpl_edc.resources | nindent 10 }}
            {{- end }}
          {{- end }}

    - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.sd_ui.projectID }}/packages/helm/stable"
      targetRevision: {{ .Values.sd_ui.targetRevision }}
      chart: {{ .Values.sd_ui.chart_name }}
      helm:
        valueFiles:
          {{- range .Values.sd_ui.valueFiles }}
          - $values/app-values/simpl-sd-ui/{{ . }}
          {{- end }}
        values: |
          ingress:
            hosts:
              - host: sd-ui.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
                paths:
                - path: /
                  pathType: ImplementationSpecific
            tls:
              - secretName: sd-ui-tld-secret
                hosts:
                  - sd-ui.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
            annotations:
              cert-manager.io/cluster-issuer: {{ .Values.cluster.issuer }}
          env:
            PUBLIC_AUTH_KEYCLOAK_SERVER_URL: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}"
            PUBLIC_CREATION_WIZARD_API_URL: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/sdtooling-api"
            PUBLIC_SIGNER_URL: "https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/signer"
          {{- if eq .Values.resourcePreset "low" }}
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
          {{- else }}
            {{- if .Values.sd_ui.resources }}
          resources: {{- toYaml .Values.sd_ui.resources | nindent 10 }}
            {{- end }}
          {{- end }}

   # START POINT

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.edc_connector_adapter.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.edc_connector_adapter.targetRevision }}
    chart: {{ .Values.edc_connector_adapter.chart_name }}
    helm:
      releaseName: {{ .Values.edc_connector_adapter.nameOverride }}
      valueFiles:
        {{- range .Values.edc_connector_adapter.valueFiles }}
        - $values/app-values/edc-connector-adapter/{{ . }}
        {{- end }}
      values: |
        ingress:
          clusterIssuer: {{ .Values.cluster.issuer }}
        domain: {{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        cluster:
          namespace: {{ .Values.cluster.namespace }}
        openapiConfig:
          servers: https://edc-connector-adapter.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        openTelemetry:
          otlpExporter:
            endpoint: http://collector.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}
        edcConnector:
          tier2BaseUrl: https://tls.participant.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/edc
          participantId: {{ .Values.cluster.namespace }}
        kafka:
          bootstrapServers: kafka.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:9092
          topics:
            transferProcess: contract_consumption.transfer-{{ .Values.cluster.namespace }}
        vault:
          secretEngine: "{{ .Values.secrets.secretEngine }}"
          secretPath: "{{ .Values.secrets.secretEngine }}/{{ .Values.cluster.namespace }}-edc-connector-adapter"
          role: "{{ .Values.secrets.role }}"
          service: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"

        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.edc_connector_adapter.resources }}
        resources: {{- toYaml .Values.edc_connector_adapter.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.files.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.files.targetRevision }}
    chart: {{ .Values.files.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.files.valueFiles }}
        - $values/app-values/files/{{ . }}
        {{- end }}
      values: |
        ingress:
          hostname: files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
          clusterIssuer: {{ .Values.cluster.issuer }}
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.files.resources }}
        resources: {{- toYaml .Values.files.resources | nindent 10 }}
          {{- end }}
        {{- end }}
      releaseName: {{ .Values.files.nameOverride }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.schema_sync_adapter.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.schema_sync_adapter.targetRevision }}
    chart: {{ .Values.schema_sync_adapter.chart_name }}
    helm:
      releaseName: {{ .Values.schema_sync_adapter.nameOverride }}
      valueFiles:
        {{- range .Values.schema_sync_adapter.valueFiles }}
        - $values/app-values/schema-sync-adapter/{{ . }}
        {{- end }}
      values: |
        ingress:
          clusterIssuer: {{ .Values.cluster.issuer }}
        domain: {{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        openapiConfig:
          servers: https://schema-sync-adapter.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        openTelemetry:
          otlpExporter:
            endpoint: http://collector.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}
        podAnnotations:
          vault.security.banzaicloud.io/vault-addr: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
          vault.security.banzaicloud.io/vault-role: {{ .Values.secrets.role }}
        postgres:
          url: "jdbc:postgresql://pg-cluster.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:5432/{{ .Values.cluster.namespace }}_schemasyncadapter?ssl=false"
          username: "{{ .Values.namespaceTag.dataprovider }}_schemasyncadapter"
          password: vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-schema-sync-adapter#SPRING_DATASOURCE_MAIN_PASSWORD
        kafka:
          auth:
            username: "{{ .Values.namespaceTag.dataprovider }}_schemasyncadapter"
            password: vault:{{ .Values.secrets.secretEngine }}/data/{{ .Values.cluster.namespace }}-schema-sync-adapter#KAFKA_AUTH_PASSWORD
          bootstrapServers: kafka.{{ .Values.cluster.commonToolsNamespace }}.svc.cluster.local:9092
          consumer:
            groupId: "{{ .Values.cluster.namespace }}.schema-sync-adapter"
          topic:
            processSchemaEvent: "{{ .Values.cluster.namespace }}.schema-sync-adapter.process-schema-event"
        schemaManager:
          serviceUrl: "https://schema-manager-be.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"
          tier2Gateway:
            url: https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}
        auth:
          serviceUrl: https://participant.be.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        webhook:
          registration:
            targetUrl: https://tls.participant.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/schema-sync-adapter
        vault:
          secretEngine: "{{ .Values.secrets.secretEngine }}"
          secretPath: "{{ .Values.secrets.secretEngine }}/{{ .Values.cluster.namespace }}-schema-sync-adapter"
          role: "{{ .Values.secrets.role }}"
          service: "https://secrets.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}"
        cluster:
          namespace: {{ .Values.cluster.namespace }}
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.schema_sync_adapter.resources }}
        resources: {{- toYaml .Values.schema_sync_adapter.resources | nindent 10 }}
          {{- end }}
        {{- end }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.sdtooling_api_be.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.sdtooling_api_be.targetRevision }}
    chart: {{ .Values.sdtooling_api_be.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.sdtooling_api_be.valueFiles }}
        - $values/app-values/sdtooling-api-be/{{ . }}
        {{- end }}
      values: |
        ingress:
          clusterIssuer: {{ .Values.cluster.issuer }}
        domain: {{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        catalogueAdapter:
          tier2Gateway:
            url: "https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"
        federatedCatalogue:
          tier2Gateway:
            url: "https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"
        openapiConfig:
          servers: https://sd-creation-wizard-api
        openTelemetry:
          otlpExporter:
            endpoint: http://collector.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}
        hash:
          config:
            algorithm: "SHA256"
            templates: '[
                          {
                            "key": "Contract Template 1",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/contract/ContractTemplate1.json"
                          },
                          {
                            "key": "Contract Template 2",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/contract/ContractTemplate2.json"
                          },
                          {
                            "key": "Contract Template 3",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/contract/ContractTemplate3.json"
                          },
                          {
                            "key": "Billing Schema 1",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/pdf/BillingSchema1.pdf"
                          },
                          {
                            "key": "Billing Schema 2",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/pdf/BillingSchema2.pdf"
                          },
                          {
                            "key": "Billing Schema 3",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/pdf/BillingSchema1.pdf"
                          },
                          {
                            "key": "Sla Agreements 1",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/pdf/SLAAgreement1.pdf"
                          },
                          {
                            "key": "Sla Agreements 2",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/pdf/SLAAgreement2.pdf"
                          },
                          {
                            "key": "Sla Agreements 3",
                            "value": "https://files.{{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}/static/pdf/SLAAgreement1.pdf"
                          }
                        ]'
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.sdtooling_api_be.resources }}
        resources: {{- toYaml .Values.sdtooling_api_be.resources | nindent 10 }}
          {{- end }}
        {{- end }}
      releaseName: {{ .Values.sdtooling_api_be.nameOverride }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.sdtooling_validation_api_be.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.sdtooling_validation_api_be.targetRevision }}
    chart: {{ .Values.sdtooling_validation_api_be.chart_name }}
    helm:
      values: |
        openTelemetry:
          otlpExporter:
            endpoint: http://collector.{{ .Values.namespaceTag.common }}.{{ .Values.domainSuffix }}
        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.sdtooling_validation_api_be.resources }}
        resources: {{- toYaml .Values.sdtooling_validation_api_be.resources | nindent 10 }}
          {{- end }}
        {{- end }}
      valueFiles:
        {{- range .Values.sdtooling_validation_api_be.valueFiles }}
        - $values/app-values/sdtooling-validation-api-be/{{ . }}
        {{- end }}
      releaseName: {{ .Values.sdtooling_validation_api_be.nameOverride }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.xfsc_advsearch_be.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.xfsc_advsearch_be.targetRevision }}
    chart: {{ .Values.xfsc_advsearch_be.chart_name }}
    helm:
      valueFiles:
        {{- range .Values.xfsc_advsearch_be.valueFiles }}
        - $values/app-values/xfsc-advsearch-be/{{ . }}
        {{- end }}
      values: |
        domain: {{ .Values.namespaceTag.dataprovider }}.{{ .Values.domainSuffix }}
        catalogueAdapter:
          tier2Gateway:
            url: "https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"
        federatedCatalogue:
          tier2Gateway:
            url: "https://tls.authority.{{ .Values.namespaceTag.authority }}.{{ .Values.authorityDomainSuffix }}"

        {{- if eq .Values.resourcePreset "low" }}
        resources:
          requests:
            cpu: "10m"
            memory: "32Mi"
        {{- else }}
          {{- if .Values.xfsc_advsearch_be.resources }}
        resources: {{- toYaml .Values.xfsc_advsearch_be.resources | nindent 10 }}
          {{- end }}
        {{- end }}
      releaseName: {{ .Values.xfsc_advsearch_be.nameOverride }}

  - repoURL: "https://code.europa.eu/api/v4/projects/{{ .Values.monitoring.projectID }}/packages/helm/stable"
    targetRevision: {{ .Values.monitoring.targetRevision }}
    chart: {{ .Values.monitoring.chart_name }}
    helm:
      releaseName: {{ .Values.monitoring.fullnameOverride }}
      valueFiles:
        {{- range .Values.monitoring.valueFiles }}
        - $values/app-values/monitoring/{{ . }}
        {{- end }}
      values: |
        clusterIssuer: {{ .Values.cluster.issuer }}
        domainSuffix: {{ .Values.domainSuffix }}
        namespaceTag: {{ .Values.namespaceTag.dataprovider }}
        mainNamespace: {{ .Values.cluster.commonToolsNamespace }}
        filebeat:
          {{- if eq .Values.resourcePreset "low" }}
          resources:
            requests:
              cpu: "10m"
              memory: "32Mi"
          {{- else }}
            {{- if .Values.monitoring.resources.filebeat }}
          resources: {{- toYaml .Values.monitoring.resources.filebeat | nindent 12 }}
            {{- end }}
          {{- end }}
